spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: system
    url: jdbc:mysql://localhost:3306/jwtRoleBased
    username: root
  jpa:
    show-sql: true
    
    hibernate:
      ddl-auto: update
    
server:
  port: 8080